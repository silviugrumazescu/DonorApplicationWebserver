export default class Doctor {
    constructor(email, password, name, cnp, district, bloodbankId) {
        this.email = email;
        this.password = password;
        this.name = name;
        this.cnp = cnp;
        this.district = district;
        this.bloodbankId = bloodbankId
    }
}